<style>.delete-btn {
  width: 80px !important;
  text-transform: uppercase;
  margin: auto;

}
  .review-card {
    margin-left: 0 !important;
    width: 100% !important;
    padding: 15px;
  }

.add1-btn{
  width: 55px !important;
  text-transform: uppercase;
  margin: auto;
  margin-right: 3px;
}

/* Make the map responsive */
#map {
  width: 100%;
  height: 300px;
  border-radius: 8px;
  overflow: hidden;
}

/* Responsive tweaks */
@media (max-width: 768px) {
  .btns .col-2,
  .btns .col-md-2 {
    width: 100% !important;
    margin-bottom: 10px;
  }

  .review-card {
    margin-left: 0 !important;
    width: 100% !important;
    padding: 15px;
  }

  .add1-btn {
    margin-bottom: 10px;
  }

  .map-container,
  .listing-card,
  .show-card {
    padding-left: 10px;
    padding-right: 10px;
  }
}
.del{
  margin-top: 12px !important;
  margin-left: -13px !important;
}

</style>


<script>
  const mapApi="<%=process.env.MAP_API%>";
const coordinates = JSON.parse("<%- JSON.stringify(listing.geometry.coordinates) %>");


</script>
<%layout("/layouts/boilerplate.ejs")-%>


<!-- Optional: wrap everything in a container -->
<div class="container my-4">

  <!-- Listing Title -->
  <div class="row">
    <div class="col-12 text-center show-card mb-3">
      <h3><%= listing.title %></h3>
    </div>
  </div>

  <!-- Listing Card -->
  <div class="row">
    <div class="card col-lg-6 col-md-8 col-sm-12 mx-auto listing-card mb-4 p-0">
      <img src="<%= listing.image.url %>" alt="img" class="card-img-top show-img" />
      <div class="card-body">
        <p class="card-text">Owned By <i><%= listing.owner.username %></i></p>
        <p class="card-text"><%= listing.description %></p>
        <p class="card-text">&#8377; <%= listing.price.toLocaleString("en-IN") %></p>
        <p class="card-text"><%= listing.location %></p>
        <p class="card-text"><%= listing.country %></p>
      </div>
    </div>
  </div>

  <!-- Edit/Delete Buttons -->
  <% if (currUser && listing.owner._id.equals(currUser._id)) { %>
    <div class="row justify-content-center btns mb-4">
      <div class="col-md-2 col-sm-6 mb-2">
        <a href="/listings/<%= listing.id %>/edit" class="btn btn-dark w-100 text-uppercase fw-bold add-btn add1-btn">Edit</a>
      </div>
      <div class="col-md-2 col-sm-6 mb-2">
        <form method="post" action="/listings/<%= listing._id %>?_method=DELETE">
          <button class="btn btn-dark w-100 text-uppercase fw-bold delete-btn">Delete</button>
        </form>
      </div>
    </div>
  <% } %>

  <!-- Review Form -->
  <div class="row">
    <div class="col-lg-6 col-md-8 col-sm-12 mx-auto mb-4">
      <% if (currUser) { %>
        <hr />
        <h3>Leave a Review</h3>
        <form action="/listings/<%= listing._id %>/reviews" method="post" novalidate class="needs-validation">
          <div class="mb-3 mt-3">
            <label for="rating" class="form-label">Rating</label>
            <fieldset class="starability-slot">
              <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="1" checked aria-label="No rating." />
              <input type="radio" id="first-rate1" name="review[rating]" value="1" />
              <label for="first-rate1" title="Terrible">1 star</label>
              <input type="radio" id="first-rate2" name="review[rating]" value="2" />
              <label for="first-rate2" title="Not good">2 stars</label>
              <input type="radio" id="first-rate3" name="review[rating]" value="3" />
              <label for="first-rate3" title="Average">3 stars</label>
              <input type="radio" id="first-rate4" name="review[rating]" value="4" />
              <label for="first-rate4" title="Very good">4 stars</label>
              <input type="radio" id="first-rate5" name="review[rating]" value="5" />
              <label for="first-rate5" title="Amazing">5 stars</label>
            </fieldset>
          </div>

          <div class="mb-3 mt-3">
            <textarea  name="review[comment]" class="form-control" id="comment" cols="30" rows="5" required></textarea>
            <div class="invalid-feedback">Please submit some comment for review</div>
          </div>
          <button class="btn btn-outline-dark">Submit</button>
          <hr />
        </form>
      <% } %>
    </div>
  </div>

  <!-- All Reviews -->
  <% if (listing.reviews.length > 0) { %>
    <div class="row g-3 mb-4">
      <p><b>All Reviews</b></p>
      <% for (review of listing.reviews) { %>
        <div class="col-md-6 col-sm-12">
          <div class="card review-card h-100">
            <div class="card-body ">
              <h5 class="card-title">@<%= review.author.username %></h5>
              <p class="starability-result card-text" data-rating="<%= review.rating %>"></p>
              <p class="card-text emoji"><%= review.comment %></p>
            </div>
            <form class="mb-3 px-3" action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="post">
              <button class="btn btn-sm btn-dark del">Delete</button>
            </form>
          </div>
        </div>
      <% } %>
    </div>
  <% } %>

  <!-- Map Section -->
  <div class="row">
    <div class="col-lg-8 col-md-10 col-sm-12 mx-auto mb-3 map-container">
      <h3>Where You Will Be</h3>
      <div id="map"></div>
    </div>
  </div>
</div>




<script src="/js/map.js"></script>
